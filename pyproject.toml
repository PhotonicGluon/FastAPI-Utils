[tool.poetry]
name = "FastAPI-Utils"
version = "0.1.0.dev1"
description = "FastAPI Utilities"
authors = ["PhotonicGluon"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9"

fastapi = "^0.115.6"
pydantic = "^2.10.3"
